{
  "name": "walletmap",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "glossary:list": "node scripts/list-glossary.mjs",
    "deploy": "npm run build && bash scripts/deploy.sh",
    "deploy:rsync": "bash scripts/deploy.sh"
  },
  "devDependencies": {
    "astro": "^5.7.10",
    "mdast-util-find-and-replace": "^3.0.2"
  },
  "dependencies": {
    "playwright": "^1.61.1",
    "rehype-mermaid": "^3.0.0"
  }
}
